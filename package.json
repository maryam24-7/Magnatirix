{
  "name": "magnatirix-server",
  "version": "1.0.0",
  "description": "Secure Encrypted Messaging System",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "prod": "NODE_ENV=production node server.js", // إزالة --enable-source-maps
    "test": "jest",
    "healthcheck": "curl -f http://localhost:$PORT/api/healthcheck || exit 1", // تعديل المسار
    "install-prod": "npm install --omit=dev",
    "build": "echo 'Building...' && npm install --omit=dev" // إضافة خطوة بناء أساسية
  },
  "dependencies": {
    // ... (كل التبعيات الحالية صحيحة)
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.0.2",
    "supertest": "^6.3.3" // إضافة لاختبارات API
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
